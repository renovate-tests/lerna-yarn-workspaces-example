{
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "prepare": "lerna run prepare",
    "clean": "rimraf packages/**/lib"
  },
  "devDependencies": {
    "@types/node": "^10.0.0",
    "ava": "^0.25.0",
    "lerna": "^2.9.0",
    "rimraf": "^2.6.2",
    "typescript": "^2.8.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
